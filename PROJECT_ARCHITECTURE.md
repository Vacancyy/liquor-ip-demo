# 酒类侵权线索智能研判工作台咨询建议书

## 一、执行摘要

本项目当前聚焦于酒类知识产权线索处理流程中的第二任务：对已经提交的疑似线索进行智能研判、风险分级和律师复核辅助。

系统不承担“主动发现线索”的任务，也不在本文档中展开线索发现、爬取、监测、视频检索或平台巡查等内容。当前系统的核心价值是：在用户已经提供图片线索及必要补充信息后，依据律师提供的判定文档、历史案例、权利基础和判词规则，形成结构化、可复核、可迭代的初筛判断。

当前系统已经具备以下能力：

- 对上传图片进行 OCR 文字识别。
- 对上传图片进行视觉要素识别。
- 基于律师判定文档进行规则匹配。
- 区分“权利产品样式”“疑似被控侵权样式”“身份不明样式”。
- 输出线索风险评分、风险等级、推荐动作和判断理由。
- 展示上传素材，便于律师核对图像与结论。
- 支持历史线索查看、重新研判和律师反馈。
- 支持规则回归检查，避免已修正误判再次回退。

建议后续重点不应继续堆叠零散规则，而应建设标准样本集、正品图库、侵权样本库、规则校准机制和人工反馈闭环，使系统从演示型初筛工具逐步升级为稳定的律师辅助研判系统。

## 二、项目背景与建设目标

### 2.1 背景

酒类侵权线索判断具有较强专业性。判断过程通常不能只看单一商标文字，也不能只看瓶型或包装局部，而需要综合考虑：

- 权利基础是否明确。
- 被控产品是否使用近似商标或近似标识。
- 酒瓶、酒盒、包装装潢是否与权利产品形成整体近似。
- 是否存在第三方标识、搭便车标识或混淆性使用。
- 是否命中历史判词中的典型视觉要素。
- 是否具备进入取证、投诉或案件流程的证据条件。

律师提供的判定文档是当前系统的主要判断依据。大模型不应替代律师规则直接作出最终法律判断，而应承担信息提取、视觉识别和辅助整理工作。

### 2.2 建设目标

当前阶段目标是建设一个“已知线索研判工作台”，即：

```txt
用户提交疑似图片线索
  ↓
系统识别图片文字和视觉要素
  ↓
系统匹配律师判定文档和历史案例规则
  ↓
系统输出风险评分、风险等级和判断理由
  ↓
律师进行复核、反馈和后续处理
```

系统目标不是直接认定侵权，也不是判断真假酒，而是为律师提供更快、更一致、更可解释的初筛结果。

## 三、当前范围界定

### 3.1 当前纳入范围

当前系统纳入以下工作范围：

- 单条图片线索上传。
- 上传素材展示。
- OCR 识别图片中文字。
- 视觉识别酒瓶、酒盒、包装装潢、商标、标签和门头等要素。
- 基于律师知识库进行权利基础和判词要素匹配。
- 生成风险评分和风险等级。
- 生成相似点、差异点、证据缺口和下一步建议。
- 展示历史线索。
- 支持重新研判。
- 支持律师反馈。

### 3.2 当前不纳入范围

以下内容不属于当前阶段重点：

- 主动发现线索。
- 自动巡检电商平台或短视频平台。
- 批量爬取网页或平台内容。
- 视频抽帧和视频线索识别。
- URL 自动抓取和页面解析。
- 自动发函、自动投诉或自动提交案件。

上述能力可作为后续独立阶段规划，但不应混入当前“已知线索研判”任务中。

## 四、现行研判业务流程

当前系统的实际研判流程如下：

```txt
1. 用户上传图片线索
   ↓
2. 用户可补充线索名称、疑似对象、来源链接和说明
   ↓
3. 系统保存线索
   ↓
4. OCR 大模型识别图片中文字、品牌词、产品名和包装文字
   ↓
5. 视觉大模型识别瓶体、瓶颈、底座、酒盒、标签、色彩和装饰线条
   ↓
6. 系统抽取第三方标识和受保护视觉要素
   ↓
7. 系统匹配律师判定文档、权利基础、历史案例和判词规则
   ↓
8. 系统形成产品身份判断
   ↓
9. 系统生成风险评分、风险等级和推荐动作
   ↓
10. 系统展示上传素材、判断结论和详细理由
   ↓
11. 律师查看报告并提交反馈
```

其中，第 7 至第 9 步是当前系统的核心。律师文档和规则优先，大模型结果作为辅助证据进入判断链路。

## 五、当前大模型应用方式

大模型在当前系统中不是只用于 OCR。经核实，当前大模型承担四类作用。

### 5.1 OCR 文字识别

OCR 模型用于识别图片中的可见文字，包括：

- 品牌词。
- 产品名。
- 酒瓶文字。
- 酒盒文字。
- 商标或 Logo 附近文字。
- 门头或包装说明文字。

OCR 结果会进入后续规则判断。例如，系统已经可以根据 OCR 识别出的“国酱”等文档高风险标识触发高风险判断。

### 5.2 视觉要素识别

视觉大模型用于识别图片中的外观和包装要素，包括：

- 圆形瓶体。
- 梯形底座。
- 细长瓶颈。
- 多层金色环圈。
- 两侧凸起装饰线条。
- 酒盒版式。
- 标签位置。
- 色彩组合。
- 第三方标识或差异标识。

这些视觉要素会被系统转化为律师文档中的判词要素命中项。

### 5.3 相似点、差异点和证据缺口辅助整理

视觉模型会输出相似点、差异点和证据缺口。系统不会直接采用模型的法律结论，但会把模型识别出的事实性内容作为辅助材料，例如：

- 瓶身正面文字与权利产品不同。
- 底座呈梯形结构。
- 瓶颈较细长。
- 酒盒采用相近竖向布局。
- 缺少购买链接或来源证据。

### 5.4 推理模型二次整理

推理模型用于在已有初筛草稿基础上进行辅助整理。它的定位是：

- 整理律师复核问题。
- 补充表达更清晰的相似点和差异点。
- 对证据缺口进行归纳。
- 辅助形成更适合律师阅读的报告语言。

推理模型不应覆盖本地律师规则生成的核心风险等级、主结论和推荐动作。

## 六、律师知识库与规则应用方式

### 6.1 知识库来源

当前系统已接入律师提供的 Word 判定资料，并完成结构化抽取。知识库包含：

- 权利基础。
- 典型案件。
- 法院判词理由。
- 包装装潢判断规则。
- 酒瓶、酒盒视觉要素。
- 被控产品 OCR 内容。
- 可复用的研判规则。

### 6.2 核心判断标准

当前系统重点使用以下判断标准：

- 是否识别到权利产品标识。
- 是否识别到第三方被控或高风险标识。
- 是否命中受保护视觉要素。
- 是否存在第三方标识与受保护视觉要素组合。
- 是否命中历史案例中的相似表达。
- 是否具备来源链接、销售场景或其他证据条件。

### 6.3 产品身份分类

当前系统会先对上传素材作产品身份判断：

```txt
更接近权利人正品 / 权利产品样式
疑似被控侵权样式
暂未识别明确产品身份
```

该分类用于避免两类常见错误：

- 把权利人正品图误判成高风险侵权。
- 把第三方标识加近似包装的被控样式误判成低风险。

### 6.4 风险评分说明

当前页面展示的百分比本质是线索风险评分，不是统计学意义上的真实侵权概率。

建议后续页面文案由“疑似侵权概率”调整为“线索风险评分”，以避免误解。

当前风险区间可理解为：

```txt
75-100：高风险
45-74：中风险
20-44：低风险
0-19：待确认或权利产品样式
```

## 七、当前已实现功能

### 7.1 图片线索研判

系统支持用户上传单张图片线索，并自动进入研判流程。

当前适用图片包括：

- 酒瓶图片。
- 酒盒图片。
- 商品截图。
- 门头或展示图片。
- 其他疑似酒类侵权材料。

### 7.2 上传素材展示

系统现在会在结果区展示上传素材。律师可以在查看风险结论时同步核对原图，避免文字报告和图片对象脱节。

### 7.3 风险结果展示

系统会输出：

- 风险评分。
- 风险等级。
- 推荐动作。
- 视觉相似度。
- 置信度。
- 证据缺口数量。

风险卡片已经按等级区分颜色：

- 高风险：红色。
- 中风险：橙色。
- 低风险：绿色。
- 待确认：灰色。

### 7.4 判断理由展示

系统会展示：

- 为什么疑似侵权。
- 产品身份判断。
- OCR 命中内容。
- 命中的律师判词视觉要素。
- 大模型辅助识别的视觉相似点。
- 与权利产品或正品样式的差异点。

### 7.5 证据缺口提示

系统会提示后续复核或取证时缺少哪些材料，例如：

- 来源链接。
- 商品页或店铺页。
- 正品图库比对。
- 商标或专利权利状态。
- 购买凭证。
- 实物照片。

### 7.6 律师反馈

系统支持律师对结果进行反馈：

- 判断合理。
- 风险偏高。
- 风险偏低。
- 需要补证。
- 无价值线索。

反馈可作为后续规则校准和样本沉淀的基础。

### 7.7 重新研判

历史线索支持重新研判。规则或模型链路调整后，可对已有线索重新生成报告。

### 7.8 回归检查

系统已经加入基础回归检查，当前用于防止已知误判样本回退。

当前检查样本包括：

```txt
Picture1.png：应保持高风险
Picture3.png：应保持高风险
```

后续应持续扩充该样本集。

## 八、当前主要问题与风险

### 8.1 风险评分尚未经过充分校准

当前百分比是规则评分，不是统计概率。不同样本之间的分值间隔仍需通过更多律师标注样本校准。

### 8.2 正品图库尚未接入

目前系统无法做真正的逐项图片比对。对于“更接近正品”或“与正品差异较小”的判断，仍依赖 OCR、视觉模型和规则推断。

### 8.3 侵权样本库规模不足

当前主要依赖律师文档中的历史案例和部分已上传样本。后续需要沉淀更多已确认侵权样本、非侵权样本和边界样本。

### 8.4 大模型耗时不稳定

视觉模型在处理图片和较长提示词时可能耗时较长。当前已经提高超时时间，但仍建议后续优化提示词长度、图片压缩策略和模型选择。

### 8.5 本地 JSON 数据库不适合生产

当前数据以本地 JSON 文件保存，适合演示和原型验证，但不适合多人协作、并发写入和生产部署。

### 8.6 律师反馈尚未自动反哺规则

当前可以保存反馈，但尚未自动将反馈转化为规则调整、样本标签或评分校准。

## 九、优化建议

### 9.1 建立标准测试样本集

建议建立三类标准样本：

```txt
正品 / 权利产品样本
确认侵权或高风险样本
边界或争议样本
```

每个样本应包含：

- 图片。
- 律师标注结论。
- 关键判断理由。
- 应命中的视觉要素。
- 应输出的风险等级。

### 9.2 完善正品图库和侵权样本库

建议建设：

- 权利产品标准图库。
- 历史侵权样本库。
- 被控产品名称和第三方标识库。
- 酒瓶/酒盒关键部位图库。

这样可以从“文字规则 + 模型描述”升级为“图像比对 + 规则判断”。

### 9.3 将百分比改为风险评分

建议将页面文案由：

```txt
疑似侵权概率
```

调整为：

```txt
线索风险评分
```

该调整更符合当前系统实际，也能避免律师或客户误解为统计概率。

### 9.4 加强报告可解释性

建议在报告中明确区分：

- OCR 命中。
- 视觉要素命中。
- 律师规则命中。
- 大模型辅助提示。
- 证据缺口。

这样可以清楚说明结论来源，降低“黑箱判断”风险。

### 9.5 扩展回归检查机制

建议将每次人工确认的误判样本加入回归检查，形成持续改进机制。

目标是避免系统在后续修改中反复出现同类错误。

### 9.6 迁移正式数据库

如果系统进入多人试用或生产阶段，建议迁移到正式数据库，并增加：

- 操作日志。
- 线索版本记录。
- 报告版本记录。
- 律师反馈记录。
- 样本标签记录。

## 十、建议实施路线

### 第一阶段：稳定当前研判能力

目标：使已知图片线索的判断更稳定、更可解释。

重点工作：

- 完善标准样本集。
- 扩充回归检查。
- 调整“概率”为“风险评分”。
- 明确报告中各类证据来源。
- 优化大模型提示词和响应时间。

### 第二阶段：建设图像对照能力

目标：提升视觉相似判断可靠性。

重点工作：

- 建设正品图库。
- 建设侵权样本库。
- 做关键视觉部位对比。
- 将图像相似检索结果纳入报告。

### 第三阶段：建设律师反馈闭环

目标：使系统具备持续学习和规则校准能力。

重点工作：

- 将律师反馈结构化。
- 建立误判原因分类。
- 将确认样本加入标准样本库。
- 定期校准风险评分规则。

### 第四阶段：生产化改造

目标：支持多人使用、稳定存储和可审计。

重点工作：

- 数据库迁移。
- 权限管理。
- 操作日志。
- 报告版本管理。
- 导出 Word 或 PDF。

## 十一、结论

当前系统已经初步完成第二任务，即“对已提交图片线索进行智能研判”的原型能力。系统能够结合 OCR、视觉识别、律师知识库、历史案例和规则评分，输出风险等级、判断理由和律师复核建议。

但当前系统仍处于原型向可用工具过渡阶段。其核心短板不在于界面或框架，而在于样本校准、正品图库、侵权样本库和回归验证体系仍需加强。

建议后续围绕“稳定研判、提升解释性、建立样本闭环”推进，而不是继续以单个误判样本为单位临时补规则。只有形成标准样本集和持续回归机制，系统才能真正具备可持续使用价值。
