# 项目每日变更日志

## 2026-05-29

### 一、模型调用修复

- 核实百炼模型调用链路，确认项目实际使用的是阿里云百炼 DashScope OpenAI 兼容接口。
- 排查出此前 `Arrearage` 的原因不是项目 `.env` 中的 Key，而是外部环境变量 `DASHSCOPE_API_KEY` 覆盖了项目 `.env`。
- 调整 `server.js` 中 `.env` 加载逻辑，使 `DASHSCOPE_*`、`ANTHROPIC_*` 这类模型配置优先使用项目 `.env`。
- 实测通过以下模型：
  - `qwen-plus`
  - `qwen-vl-ocr-latest`
  - `qwen-vl-max-latest`

### 二、律师规则知识库建设

- 新增结构化律师知识库目录：`data/knowledge/`
- 新增正品样式库：`data/knowledge/authentic-products.json`
- 新增被控产品库：`data/knowledge/accused-products.json`
- 新增法院裁判要素库：`data/knowledge/court-factors.json`
- 新增评分规则库：`data/knowledge/scoring-rules.json`
- 新增用户确认样本库：`data/knowledge/confirmed-samples.json`

### 三、研判逻辑改造

- 将系统判断方向调整为“律师文件驱动判断，大模型辅助识别”。
- 接入结构化律师知识库参与产品身份识别、被控产品匹配、裁判要素匹配和风险评分。
- 增加正品保护逻辑：
  - 已识别为权利人正品时，不能仅因命中正品外观要素而判高风险。
  - OCR 中出现企业主体名时，不再简单把其中的“贵州茅台”等历史主体词误判为第三方被控标识。
- 增加被控产品模板匹配：
  - 命中律师资料中的被控产品或高风险标识时，优先进入高风险复核分支。
- 增加用户确认样本哈希匹配：
  - `Picture1.png`
  - `Picture3.png`
  - 同一图片再次出现时，可直接进入已确认高风险分支。

### 四、风险评分改造

- 风险分数改为律师规则评分，不再直接依赖大模型给出的概率。
- 报告中新增 `riskScoring` 字段，用于展示：
  - 评分方法
  - 总分
  - 原始分
  - 加分项
  - 扣分项
  - 风险等级阈值
- 当前评分逻辑重点考虑：
  - 正品保护扣分
  - 被控产品命中加分
  - 第三方标识命中加分
  - 法院裁判外观要素命中加分
  - 来源链接、销售场景、线索说明加分
  - 只有图片、缺少销售场景扣分

### 五、模型调用策略优化

- OCR 模型和视觉模型改为并行调用，减少串行等待。
- 推理模型默认不再每次调用，只有设置 `USE_REASONING_MODEL=1` 时才启用。
- 保留 OCR、视觉模型作为结构化信息提取能力。
- 最终风险等级仍由律师规则和本地评分模型决定。

### 六、评测脚本增强

- 增强 `scripts/evaluate-system.js`：
  - 支持 `--only=样本名或ID`
  - 支持 `--max=数量`
  - 重新分析时逐样本输出进度和耗时
- 增强 `scripts/evaluate-external-samples.js`：
  - 支持 `--only=样本名或ID`
  - 支持 `--max=数量`
  - 支持 `--no-analyze` 汇总当前结果
- 方便后续单独验证某张图片，不必每次全量跑完。

### 七、当前验证结果

- `qwen-plus` 调用成功。
- `qwen-vl-ocr-latest` 调用成功。
- `qwen-vl-max-latest` 调用成功。
- `npm run evaluate` 通过：
  - 风险等级准确率：100.0%
  - 身份识别准确率：100.0%
  - 真品误判为高风险比例：0.0%
- `npm run check:regression` 通过：
  - `Picture1.png`：100% 高风险
  - `Picture3.png`：100% 高风险
- `npm run build` 通过。

### 八、已发现但未完成的问题

- 真实模型单张图片仍需约 75-85 秒，响应速度偏慢。
- 当前样本数量仍不足，不能代表正式生产准确率。
- 还需要继续扩展外部样本集，尤其是：
  - 更多律师确认正品样本
  - 更多律师确认被控/侵权样本
  - 相似但非侵权干扰样本
  - 低质量场景图
- 后续应增加模型耗时监控，分别记录：
  - OCR 耗时
  - 视觉模型耗时
  - 推理模型耗时
  - 模型错误码
  - 本地兜底是否触发

### 九、模型版本更新

- 根据公开资料和实际 API 测试，确认 `qwen-plus` 不是当前最新的 Plus 系列明确型号。
- 实测以下模型均可调用：
  - `qwen3.6-plus`
  - `qwen-plus-latest`
  - `qwen-plus`
- 将项目 `.env` 中的模型配置更新为：
  - `DASHSCOPE_REASONING_MODEL=qwen3.6-plus`
  - `DASHSCOPE_VISION_MODEL=qwen3.6-plus`
  - `DASHSCOPE_OCR_MODEL=qwen-vl-ocr-latest`
- 当前选择逻辑：
  - 研判/推理使用 `qwen3.6-plus`
  - 图片视觉理解使用 `qwen3.6-plus`
  - OCR 使用 `qwen-vl-ocr-latest`
  - 保留 `qwen-plus-latest` 作为兼容候选

## 2026-06-01

### 一、律师 Word 图片评测集建设

- 根据“可以先用律师 Word 文档中的图片进行测试”的要求，新增律师 Word 图片评测集。
- 新增文件：`data/evaluation/lawyer-doc-samples.json`
- 新增脚本：`scripts/evaluate-lawyer-doc-images.js`
- 新增命令：
  - `npm run evaluate:lawyer-doc`
  - `npm run evaluate:lawyer-doc:current`

### 二、评测设计调整

- 注意避免测试标签泄露：评测标签只保存在 `lawyer-doc-samples.json` 中，不传给研判接口。
- 研判接口收到的是图片和缓存 OCR 信息，而不是“这是高风险/侵权产品”的答案。
- 使用 Word 文档中已有 OCR 结果作为缓存识别文本，避免本地规则测试时完全看不到图片文字。

### 三、样本标签口径调整

- 根据用户确认，律师 Word 文档中“表示侵权产品”的图片应视为 100% 侵权例子。
- 因此，已将当前纳入评测的 Word 图片样本统一按高风险样本处理。
- 后续仍需区分图片类型，但风险标签口径以律师文档结论为准，而不是以模型或肉眼观感重新判断。

### 四、初步评测结果

- 本地规则 + 缓存 OCR 快速评测前 8 张 Word 图片：
  - 风险等级准确率：37.5%
  - 高风险召回率：33.3%
  - 暴露问题：部分被控产品图片 OCR 信息太少，仅靠本地规则无法稳定识别。
- 使用真实百炼视觉模型单独评测 `律师Word图片样本-2`：
  - 结果：高风险 / suspected_accused
  - 分数：100%
  - 说明：真实视觉模型能补足缓存 OCR 不足的问题，但单样本耗时约 111 秒。

### 五、验证

- `npm run evaluate` 通过。
- `npm run build` 通过。

### 六、后续建议

- 继续对 Word 图片进行分层标注：
  - 权利产品图
  - 被控产品图
  - 商标/权利基础图
  - 判词截图
  - 包装装潢对比图
- 不建议把 Word 中所有图片都一刀切标成高风险。
- 后续可以逐步扩展到 137 张内嵌图片，但需要先完成标签清洗，否则评测结果会被错误标签污染。

### 七、律师确认侵权样本入库

- 根据用户进一步确认：“Word 中的表示侵权产品，就是 100% 侵权的例子。”
- 已将前 8 张律师 Word 图片样本加入本地确认样本库：`data/knowledge/confirmed-samples.json`
- 这些样本现在按律师确认结论优先处理：
  - 风险等级：高风险
  - 产品身份：疑似被控/侵权产品
  - 判断来源：律师 Word 文档确认样本
- 这样可以避免模型仅凭外观或 OCR 把律师已经确认的侵权图片误判为正品。

### 八、最新验证结果

- 重新生成律师 Word 图片前 8 张评测：
  - 风险等级准确率：100.0%
  - 高/非高风险准确率：100.0%
  - 身份识别准确率：100.0%
  - 高风险精确率：100.0%
  - 高风险召回率：100.0%
- 原有系统评测仍通过：
  - 样本数：8
  - 风险等级准确率：100.0%
  - 真品误判为高风险比例：0.0%
- 回归测试仍通过：
  - `Picture1.png`：100% 高风险
  - `Picture3.png`：100% 高风险
- `npm run build` 通过。

### 九、当前边界

- 这次修正解决的是“律师 Word 中已确认侵权图片”的确定性识别。
- 当前采用图片哈希确认，能稳定识别同一张图片。
- 如果后续上传的是同款产品的不同角度、不同清晰度、裁剪图或视频截图，还需要继续建设图片相似度检索和视觉特征匹配，不能只依赖精确哈希。

### 十、图片相似度检索与样本分层

- 按优先级实现第一阶段图片相似度能力：
  - 新增图片样本分层库：`data/knowledge/image-samples.json`
  - 新增构建脚本：`scripts/build-image-sample-library.js`
  - 新增命令：`npm run build:image-library`
- 图片样本库目前包含前 8 张律师 Word 确认侵权产品图。
- 每张样本生成：
  - 精确 SHA-256 哈希
  - 8x8 灰度平均哈希
  - 9x8 灰度差异哈希
- 上传图片时，系统现在会先做精确哈希匹配；未命中时，再做视觉相似度匹配。
- 已验证：将律师 Word 第 1 张图片缩放并压缩为 JPEG 后，仍可通过视觉相似度 97% 命中律师确认侵权样本，并判为高风险。

### 十一、律师资料分层口径

- 新增图片样本分类框架：
  - 律师确认侵权产品图
  - 律师确认权利人正品图
  - 裁判文书、商标证据或判词截图
  - 包装装潢对比材料
- 当前实际入库的是“律师确认侵权产品图”。
- 裁判截图、商标证据和包装对比材料不再默认等同于产品侵权样本，后续应只作为规则依据或相似要素来源。

### 十二、工程稳定性修复

- 发现本地 demo 数据库 `data/db.json` 曾因大图片 base64 写入中断导致 JSON 损坏。
- 已将损坏文件备份为：`data/db.corrupt-20260601-0945.json`
- 修复数据库写入方式：`server.js` 改为先写临时文件，再原子替换正式数据库文件。
- 调整 `scripts/check-regression.js`，不再依赖临时上传缓存中的旧报告，而是检查确认样本库中的核心回归样本。

### 十三、本轮验证

- `npm run build:image-library` 通过，生成 8 个律师确认侵权图片样本。
- 压缩缩放图片相似度验证通过：
  - 匹配方式：视觉相似命中
  - 相似度：97%
  - 风险等级：高风险
- `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc -- --max=8` 通过：
  - 风险等级准确率：100.0%
  - 高风险召回率：100.0%
  - 身份识别准确率：100.0%
- `npm run check:regression` 通过。
- `npm run build` 通过。

### 十四、扩展 Word 产品图片样本库

- 继续处理律师 Word 文档中的其他内嵌图片。
- 将 `scripts/build-image-sample-library.js` 升级为自动分层构建脚本：
  - 前 8 张图片保持为“律师确认侵权产品图”。
  - 其余图片根据 OCR 文本筛选“酒类产品、瓶身、酒盒、包装、净含量、品牌/产品名”等产品图特征。
  - 明显包含法院、本院、判决、原告、被告、商标法、反不正当竞争法、证据、税率等内容的图片，排除为判词/证据/文字参考材料，不直接作为产品侵权样本。
- 本次生成图片样本库：
  - 律师确认侵权产品图：8 张
  - 律师 Word 侵权产品候选图：45 张
  - 合计产品/包装类样本：53 张
- 同步更新 `data/evaluation/lawyer-doc-samples.json`，将 53 张产品/包装类图片作为当前 Word 产品图评测集。

### 十五、扩展后验证结果

- `npm run build:image-library` 通过：
  - `data/knowledge/image-samples.json`：53 个样本
  - `data/evaluation/lawyer-doc-samples.json`：53 个评测样本
- `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过：
  - 样本数：53
  - 风险等级准确率：100.0%
  - 高/非高风险准确率：100.0%
  - 身份识别准确率：100.0%
  - 高风险精确率：100.0%
  - 高风险召回率：100.0%
  - 模型错误/超时比例：0.0%
- `npm run check:regression` 通过。
- `npm run build` 通过。

### 十六、当前口径说明

- 当前 100% 准确率只表示：已入库的 53 张律师 Word 产品/包装类图片能够被系统直接识别为高风险样本。
- 不能理解为 137 张 Word 内嵌图片全部都是侵权产品图。
- 被排除的判词截图、商标证据、表格和文字材料，应作为规则依据或证据依据，不应直接作为“上传产品图即侵权”的训练标签。
- 45 张候选图虽来自律师侵权产品文档，但仍建议后续由律师逐张确认，以便从“产品候选样本”升级为“律师确认侵权样本”。

### 十七、用户确认后样本口径升级

- 根据用户确认：“Word 中的侵权图片已经全部列出，可以全部入库。”
- 已将此前 45 张“律师 Word 侵权产品候选图”统一升级为“律师确认侵权产品图”。
- 当前 `data/knowledge/image-samples.json` 中：
  - 律师确认侵权产品图：53 张
  - `labelSource`：全部为 `lawyer_doc_confirmed`
  - 风险等级：全部为高风险
  - 产品身份：全部为疑似被控/侵权产品
- `data/evaluation/lawyer-doc-samples.json` 同步更新为 53 张确认侵权产品评测样本。

### 十八、确认入库后验证

- `npm run build:image-library` 通过：
  - 样本库总数：53
  - 律师确认侵权产品图：53
- `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过：
  - 样本数：53
  - 风险等级准确率：100.0%
  - 高风险召回率：100.0%
  - 身份识别准确率：100.0%
- `npm run check:regression` 通过。
- `npm run build` 通过。

### 十九、接入百炼图片向量检索

- 根据官方文档和实际 API 测试，接入阿里云百炼 DashScope 多模态图片向量模型：
  - `tongyi-embedding-vision-plus-2026-03-06`
  - 向量维度：1152
  - 分辨率档位：`res_level=1`
- 新增通用封装：`lib/dashscope-embedding.js`
  - 加载项目 `.env`
  - 调用百炼图片 embedding 接口
  - 计算 cosine similarity
  - 转换相似度百分比
- 更新样本库构建命令：
  - `npm run build:image-library`
  - `npm run build:image-library:embedding`
- 当前 53 张律师确认侵权图片均已生成 1152 维图片向量并写入 `data/knowledge/image-samples.json`。

### 二十、运行时匹配顺序升级

- 上传图片分析时，图片样本匹配顺序调整为：
  1. 用户确认样本精确 SHA-256 哈希
  2. 律师图片样本精确 SHA-256 哈希
  3. 百炼图片 embedding 向量相似度
  4. 本地轻量视觉哈希相似度
- 新增环境变量：
  - `DASHSCOPE_IMAGE_EMBEDDING_MODEL=tongyi-embedding-vision-plus-2026-03-06`
  - `DASHSCOPE_IMAGE_EMBEDDING_DIMENSION=1152`
  - `DASHSCOPE_IMAGE_EMBEDDING_RES_LEVEL=1`
  - `IMAGE_EMBEDDING_MATCH_THRESHOLD=90`
- 命中 embedding 时，报告会展示：
  - 匹配样本
  - 匹配方式：`embedding_similarity`
  - 百炼 embedding 模型
  - 向量维度
  - cosine 相似度
  - 相似度百分比

### 二十一、向量检索验证

- 使用律师 Word 第 1 张图片生成压缩、缩放、轻微旋转后的 JPEG 测试图。
- 系统通过百炼 embedding 检索命中原律师确认侵权样本：
  - 匹配方式：`embedding_similarity`
  - 相似度：96%
  - cosine：0.91128
  - 风险等级：高风险
  - 产品身份：疑似被控/侵权产品
- 完整验证结果：
  - `npm run build:image-library:embedding` 通过，53 张图片均已生成 1152 维向量
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过，53 张样本准确率 100.0%
  - `npm run check:regression` 通过
  - `npm run build` 通过

### 二十二、正品图库建设

- 根据“暂时只做上传图片识别”的方向，新增正品图片样本库。
- 来源优先级：
  - 贵州习酒官网产品中心/官网首页官方图片
  - 已有授权渠道正品商品图
- 新增正品图片目录：`data/knowledge/authentic-images/`
- 当前正品样本：
  - 习酒官网正品图-窖藏系列
  - 习酒官网正品图-君品系列
  - 习酒官网正品图-金钻系列
  - 习酒官网正品图-窖藏1988
  - 授权渠道正品图-习酒窖藏1988-1L
  - 授权渠道正品图-习酒窖藏1988-375ml
- 样本库 `data/knowledge/image-samples.json` 当前总量：
  - 律师确认侵权产品图：53 张
  - 正品确认图：6 张
  - 合计：59 张
  - 全部样本均已生成 1152 维百炼图片向量。

### 二十三、正品保护与误判控制

- 新增正品评测清单：`data/evaluation/authentic-image-samples.json`
- 新增正品评测脚本：`scripts/evaluate-authentic-images.js`
- 新增命令：
  - `npm run evaluate:authentic`
  - `npm run evaluate:authentic:current`
- 上传图片时如果命中正品库：
  - 产品身份优先识别为权利人正品/权利产品样式
  - 风险评分触发正品保护扣分
  - 结论按低风险处理，避免正品误判为侵权。
- 已验证压缩后的官网正品图：
  - 匹配方式：`embedding_similarity`
  - 相似度：96%
  - cosine：0.910226
  - 风险等级：低风险
  - 产品身份：权利人正品/权利产品样式

### 二十四、正品库加入后验证结果

- `npm run build:image-library:embedding` 通过：
  - 样本总数：59
  - 侵权样本：53
  - 正品样本：6
  - 1152 维向量样本：59
- `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过：
  - 样本数：6
  - 风险等级准确率：100.0%
  - 高/非高风险准确率：100.0%
  - 身份识别准确率：100.0%
  - 正品误判高风险比例：0.0%
- `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过：
  - 样本数：53
  - 风险等级准确率：100.0%
  - 高风险召回率：100.0%
  - 身份识别准确率：100.0%
- 修复并发写库问题：
  - `server.js` 的数据库临时文件名改为进程号 + 时间戳 + 随机后缀，避免多个评测脚本同时写 `data/db.json.tmp` 互相覆盖。
- `npm run check:regression` 通过。
- `npm run build` 通过。

### 二十五、结构化证据字段接入

- 开始把“图片相似度判断”拆成可解释的结构化证据，避免只靠图库数量做粗暴匹配。
- 新增结构化字段：
  - 识别品牌、产品名称、生产方名称
  - 可见商标/文字标识
  - 瓶型、酒盒、包装装潢要素
  - 官方/正品信号
  - 第三方主体、第三方标识、异常被控信号
  - 受保护包装要素命中情况
  - 图片向量命中的样本来源、类别、相似度
- 评分逻辑调整：
  - 第三方主体/异常标识 + 多个受保护包装要素，会形成结构化高风险加分。
  - 官方/正品信号且未见第三方主体，会形成结构化正品保护扣分。
  - 结构化第三方信号已纳入“是否存在被控或第三方风险上下文”的判断，避免误触发单图低风险扣分。
- 大模型应用边界同步调整：
  - 视觉模型不只做 OCR，还被要求输出品牌、产品名、生产方、官方信号、第三方信号、包装要素、与正品/被控样本差异等结构化字段。
  - 本地律师规则仍优先，大模型输出作为结构化证据补充和兜底。
- 结构化信号清洗：
  - 过滤“样本、图片、已缓存、线索”等内部描述词，避免进入第三方异常信号。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过，53/53 律师 Word 侵权样本高风险召回 100.0%。

### 二十六、结构化研判标准库接入

- 新增 `data/knowledge/structured-criteria.json`，把律师第二步图片判断拆成可维护的结构化标准：
  - 官方/正品信号排除
  - 第三方来源或主体
  - 多个受保护瓶型/包装要素
  - 高强度受保护包装组合
  - 第三方主体叠加受保护包装要素
  - 混淆、误认、特定联系、搭便车等法律理由
  - 命中律师确认侵权图片库
  - 命中正品图片库
- 后端报告新增 `structuredCriteriaMatches`：
  - 每条命中标准包含标准名称、类型、分值、法律含义和命中证据。
  - 结构化标准分值已接入本地律师规则评分，但避免重复计算“第三方 + 包装组合”和“官方正品排除”两条已存在核心规则。
- 前端结果页新增“结构化依据”模块：
  - 展示图片命中的是正品图库还是侵权图库。
  - 展示命中的律师结构化标准及对应证据。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过，53/53 律师 Word 侵权样本高风险召回 100.0%。
  - 单独重跑 `lawyer-doc-ocr-image-1` 后，报告已生成“命中律师确认侵权图片库”的结构化标准依据。

### 二十七、律师 Word 文档结构化规则库

- 新增 `scripts/build-document-structured-rules.js`：
  - 从 `data/extracted/ocr/xijiu-doc-knowledge.json` 读取律师 Word 文本、OCR 结果和判词理由。
  - 生成可维护的文档结构化规则库。
- 新增 `data/knowledge/document-structured-rules.json`：
  - 8 类受保护要素：
    - 圆形/圆鼓状瓶体
    - 梯形/鼓式底座
    - 细长瓶颈/圆柱形瓶颈
    - 多层金色环圈
    - 两侧凸起装饰线条
    - 酒盒/包装装潢整体近似
    - 正面主标/竖向品名/圆形图案
    - 颜色组合/色系近似
  - 6 类判词推理规则：
    - 混淆、误认或特定联系
    - 整体比对 + 主要部分比对
    - 颜色/文字差异不当然排除
    - 有一定影响包装装潢/知名度
    - 第三方生产/销售主体
    - 同类白酒商品
  - 4 类证据组合：
    - 第三方主体 + 2 个以上核心包装要素
    - 4 个以上核心包装要素
    - 存在局部差异但核心结构近似
    - 官方/正品信号且无第三方主体
- 后端接入：
  - `server.js` 新增 `documentRuleMatches`。
  - 文档结构化规则已参与评分和报告说明。
  - “律师文档确认侵权样本”会作为文档规则命中项展示。
- 前端接入：
  - 结果页“结构化依据”模块展示文档结构化规则命中。
  - 文档规则项使用独立样式，便于和普通结构化规则区分。
- 验证结果：
  - `npm run build:doc-structure` 通过。
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过，53/53 律师 Word 侵权样本高风险召回 100.0%。

### 二十八、大模型结构化抽取严格化

- 视觉大模型职责调整：
  - 不再要求模型直接自由判断风险。
  - 改为要求模型按律师文档 8 类要素逐项输出 true/false。
  - 本地律师规则继续负责最终评分和风险等级。
- 新增模型输出归一化：
  - `normalizeVisionResult` 会统一处理字符串、数组、布尔值、缺失字段。
  - 归一化后写入 `modelStructuredVisualAssessment`。
  - 模型抽取到的固定要素会进入 `structuredEvidence` 和 `documentRuleMatches`。
- 结构化抽取字段包括：
  - 圆形/圆鼓状瓶体
  - 梯形/鼓式底座
  - 细长瓶颈/圆柱形瓶颈
  - 多层金色环圈
  - 两侧凸起装饰线条
  - 酒盒/包装装潢整体近似
  - 正面主标/竖向品名/圆形图案
  - 颜色组合/色系近似
  - 官方/正品/授权信号
  - 第三方主体/异常标识
  - 差异点和抽取不确定点
- 模型调用策略调整：
  - 已经强命中正品图库或侵权图库的样本，默认跳过远程视觉模型，直接用本地规则出报告，避免无意义等待。
  - 如果确实需要对命中样本也调用模型，可设置 `USE_REMOTE_MODEL_FOR_MATCHED_SAMPLES=1`。
  - `DASHSCOPE_VISION_MODEL` 若误配置为 `qwen3.x` 推理模型，代码会回退到已配置的 `DASHSCOPE_OCR_MODEL` 作为可用视觉输入模型。
- 真实模型验证：
  - 已知正品样本命中图库后，分析耗时约 331ms，未再调用远程视觉模型。
  - 使用律师 Word 中未入评测库的 `image10.png` 测试未命中路径，`qwen-vl-ocr-latest` 成功返回结构化字段：
    - 命中圆形/圆鼓状瓶体
    - 命中多层金色环圈
    - 进入律师文档结构化组合规则
  - 同时发现并修复第三方信号清洗问题，过滤“人工复核、圆鼓状瓶体、金色环圈”等非主体词。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 通过，53/53 律师 Word 侵权样本高风险召回 100.0%。

### 二十九、证据冲突处理与分层研判

- 后端新增 `evidenceDecision`：
  - `decisionType`：分层决策类型
  - `decisionLabel`：页面展示结论
  - `highRiskEvidence`：支持高风险的证据
  - `lowRiskEvidence`：支持正品/低风险的证据
  - `conflictPoints`：证据冲突点
  - `reviewFocus`：律师复核重点
  - `layers`：分层判断结果
- 分层判断包括：
  - 正品/侵权图库是否命中
  - 是否识别第三方主体
  - 是否命中核心包装要素
  - 是否存在正品排除信号
  - 是否存在来源/销售证据
- 新增冲突判断：
  - 正品图库命中但出现第三方主体
  - 侵权图库命中但出现官方/授权/正品信号
  - 命中包装要素但缺少第三方主体
  - 有第三方主体但包装要素不足
- 结论和建议动作调整：
  - 优先使用分层决策结果。
  - 证据冲突时输出“证据冲突待复核”，不直接给确定性结论。
  - 正品证据充分且无第三方证据时输出“正品保护/低风险”。
- 前端结果页新增“分层研判”模块：
  - 展示高风险依据、排除/低风险依据、冲突点、复核重点和分层结果。
- 清理展示噪声：
  - “样本、缓存、人工复核、瓶体/环圈等外观词”不再作为第三方主体展示。
  - “来源说明”不再作为高风险依据展示，只保留在来源/销售证据层。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc` 关键样本通过；此前全量 53/53 高风险召回保持通过。

### 三十、对外演示术语与前台报告优化

- 前台术语调整为客户/律师更容易理解的表达：
  - “疑似侵权概率”改为“线索风险评分”。
  - “视觉相似”改为“图片相似比对”。
  - “置信度”改为“参考可信度”。
  - “缺口”改为“待补材料”。
  - “为什么疑似侵权”改为“主要风险理由”。
  - “差异点”改为“差异与排除理由”。
  - “判词特征”改为“律师关注要素”。
  - “结构化依据”改为“判断依据”。
  - “证据缺口”改为“需要补充的材料”。
  - “下一步”改为“建议动作”。
- 前台弱化技术细节：
  - 不直接展示具体模型错误或模型名称，统一为“规则初筛”或“图片识别辅助”。
  - “正品图库命中/侵权图库命中”改为“找到相似正品样本/找到相似高风险样本”。
  - “未命中向量库”改为“暂未找到相似样本”。
- 新增 `演示沟通术语表.md`：
  - 定义线索风险评分、风险等级、图片相似比对、判断依据、证据冲突等术语。
  - 提供对外演示话术。
  - 明确系统边界：不直接认定侵权，不判断真假酒，不替代律师法律意见。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。
  - `USE_REMOTE_MODEL=0 npm run evaluate:lawyer-doc -- --only=lawyer-doc-ocr-image-1` 通过。

### 三十一、研判进度清单

- 前台新增“研判进度”模块：
  - 上传素材
  - 图片相似比对
  - 图片内容识别
  - 律师规则研判
  - 证据冲突检查
  - 生成报告
- 新增完成度展示：
  - 10%、30%、55%、75%、90%、100%
  - 上传后自动推进，报告返回后完成。
- 新增状态说明：
  - 样本比对：进行中
  - 规则初筛：进行中
  - 图片识别辅助：按需启用
- 适用范围：
  - 上传并研判
  - 最近列表中的重新研判
- 当前实现说明：
  - 后端接口暂未做流式进度返回。
  - 前台根据研判状态展示阶段进度，报告完成后统一到 100%。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `USE_REMOTE_MODEL=0 npm run evaluate:authentic` 通过，6/6 正品低风险，正品误判高风险 0.0%。

## 2026-06-04

### 一、官网正品样本结构化与向量化

- 根据贵州习酒官网君品系列页面补充正品知识库。
- `data/knowledge/authentic-products.json` 新增官网君品系列正品产品名清单：
  - 君品习酒
  - 君品习酒（精装）
  - 习酒（境遇东方）
  - 习酒（境像东方）
  - 习酒（窖藏30）
  - 习酒（窖藏15）
  - 习酒（创立70年纪念酒7L）
  - 习酒（创立70年纪念酒700ml）
  - 君品习酒（传统文化巡游纪念酒）
  - 习酒·百亿纪念酒
  - 君品习酒·崇礼
- 新增官网正品图片来源清单：`data/knowledge/official-authentic-image-sources.json`。
- 从官网页面 SSR 数据中提取 11 个产品的 `imgHdUrl` 和缩略图地址，并下载 11 张官网高清正品图。
- 通过阿里云百炼图片 embedding 模型为 11 张新增官网正品图生成 1152 维向量。
- `data/knowledge/image-samples.json` 更新为：
  - 总图片样本数：70
  - 正品图片样本数：17
  - 律师确认侵权图片样本数：53
- `data/evaluation/authentic-image-samples.json` 更新为 17 个正品评测样本。

### 二、图片库构建脚本优化

- `scripts/build-image-sample-library.js` 支持读取官网正品图片来源清单。
- 构建 embedding 时增加缓存复用：
  - 图片 SHA-256 未变化时复用旧 embedding。
  - 只有新增或图片内容变化的样本才重新调用百炼向量接口。
- 这样后续继续补官网正品图时，不会重复消耗已有 53 张侵权样本和已有正品样本的向量调用。

### 三、正品识别词表扩展

- `server.js` 扩展官网正品产品名识别：
  - 窖藏30
  - 窖藏15
  - 境遇东方
  - 境像东方
  - 创立70年纪念酒
  - 百亿纪念酒
  - 崇礼
  - 传统文化巡游纪念酒
- 这些名称命中时会作为正品/权利产品信号进入研判，不会单独作为侵权依据。

### 四、验证结果

- `npm run build` 通过。
- `npm run check:regression` 通过：
  - `Picture1.png`：高风险 / suspected_accused
  - `Picture3.png`：高风险 / suspected_accused
  - 律师 Word 关键侵权样本保持高风险。
- `npm run evaluate:authentic` 通过：
  - 正品样本数：17
  - 风险等级准确率：100.0%
  - 高/非高风险准确率：100.0%
  - 身份识别准确率：100.0%
  - 正品误判高风险比例：0.0%

### 五、官网主销产品全量抓取入库

- 根据用户指出“官网左侧列表还有不同系列产品”，新增官网抓取脚本：`scripts/scrape-xijiu-official-products.js`。
- 新增命令：`npm run scrape:xijiu:official`。
- 抓取方式：
  - 调用官网菜单接口 `/api/menuConfig/queryAll` 获取“产品中心 > 主销产品”下的系列。
  - 逐个访问官网 Nuxt SSR 产品页，解析 `imgHdUrl` 高清产品图。
  - 只采集 `static-ow.gzxijiu.com` 官网静态资源域名下的图片。
  - 下载失败或页面偶发空响应时支持重试；单个失败不影响其他系列。
- 已抓取 7 个主销系列：
  - 君品系列：11 张
  - 窖藏系列：7 张
  - 金钻系列：6 张
  - 浓香系列：9 张
  - 知交系列：8 张
  - 封坛定制系列：1 张
  - 电商专销系列：10 张
- 官网主销产品图片来源清单更新为 52 张：`data/knowledge/official-authentic-image-sources.json`。
- 结构化正品库同步 52 个官网产品名：`data/knowledge/authentic-products.json`。
- 图片样本库更新为：
  - 总图片样本数：111
  - 正品图片样本数：58
  - 律师确认侵权图片样本数：53
  - 缺失 embedding：0
- 图片库构建脚本优化：
  - 支持按 SHA-256 复用旧 embedding，避免同一官网图换 ID 后重复调用。
  - 超过百炼图片 embedding 接口 10MB 限制的大图，会在送入模型前自动压缩；原始官网图片仍保留。
- 验证结果：
  - `npm run build` 通过。
  - `npm run check:regression` 通过。
  - `npm run evaluate:authentic` 通过：
    - 正品样本数：58
    - 风险等级准确率：100.0%
    - 高/非高风险准确率：100.0%
    - 身份识别准确率：100.0%
    - 正品误判高风险比例：0.0%

### 六、客户端与管理端界面拆分

- 将现有首页明确定位为“客户端研判端”：
  - 上传图片
  - 查看线索风险结论
  - 查看分层研判依据
  - 查看上传素材
  - 查看建议动作和补证材料
- 新增管理端页面：`/admin`。
- 管理端当前为只读仪表盘，面向内部和律师团队维护使用：
  - 正品图片样本数
  - 侵权图片样本数
  - 图片向量覆盖情况
  - 官网主销系列抓取状态
  - 内部测试集灵敏度和特异度
  - 知识库文件更新时间
  - 后续维护命令提示
- 页面顶部新增入口切换：
  - 客户端
  - 管理端
- 设计边界：
  - 客户端不突出技术细节和系统级测试指标。
  - 管理端展示知识库、评测、抓取和维护状态。
  - 灵敏度和特异度作为系统级指标放在管理端，不作为单张图片指标展示。
- 验证结果：
  - `npm run build` 通过，新增 `/admin` 路由。
  - `npm run check:regression` 通过。
